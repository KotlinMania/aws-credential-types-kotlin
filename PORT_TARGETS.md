# Port targets — `aws-credential-types-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `aws-credential-types` that codex-rs uses, and therefore the minimum subset that `aws-credential-types-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-20] (rank `159`, tier `direct`, status `To Do`)

**Cargo package name**: `aws-credential-types`
**Rust module name (in `use` statements)**: `aws_credential_types`

## Summary

- **4** distinct symbols imported by codex-rs
- **6** total `use` statements
- **3** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `aws_credential_types::provider::SharedCredentialsProvider`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-aws-auth`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `aws-auth/src/config.rs:3` (`codex-aws-auth`) — `use aws_credential_types::provider::SharedCredentialsProvider`
- `aws-auth/src/lib.rs:7` (`codex-aws-auth`) — `use aws_credential_types::provider::SharedCredentialsProvider`

### `aws_credential_types::Credentials`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-aws-auth`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `aws-auth/src/lib.rs:140` (`codex-aws-auth`) — `use aws_credential_types::Credentials`
- `aws-auth/src/signing.rs:3` (`codex-aws-auth`) — `use aws_credential_types::Credentials`

### `aws_credential_types::provider::ProvideCredentials`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-aws-auth`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `aws-auth/src/lib.rs:5` (`codex-aws-auth`) — `use aws_credential_types::provider::ProvideCredentials`

### `aws_credential_types::provider::error::CredentialsError`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-aws-auth`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `aws-auth/src/lib.rs:142` (`codex-aws-auth`) — `use aws_credential_types::provider::error::CredentialsError`
